Fixes #

<!-- Terse (see CONTRIBUTING.md): what changed as a short list, then how it was verified. The why lives in the commit messages. -->

-

**Verified:**

<!--
Tick only what applies:
- [ ] Regression test in the domain's test project, chaos-tested (fails on the guarding assertion with the bug re-applied)
- [ ] Golden hashes untouched, or the index rebuilt on win-x64 (say why)
- [ ] README or docs updated if behaviour changed

Override labels, if a check fails on purpose: contract-change, tests-removed, coverage-drop.
-->
