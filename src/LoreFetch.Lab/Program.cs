// LoreFetch.Lab — index-build tooling entry point.
// Stream B (package B1+) owns the real implementation; this is the bare
// shell S0.1 needs so the solution compiles and links against Core.
Console.WriteLine("LoreFetch.Lab — nothing to do yet (Stream B owns this).");
